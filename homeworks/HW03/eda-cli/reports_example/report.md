# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Качество данных (эвристики)

- Оценка качества: **0.59**
- Макс. доля пропусков: **5.56%**
- Порог проблемных колонок по пропускам: **30.00%**
- Максимум гистограмм: **6**
- top-k категорий: **5**

## Проблемные колонки по пропускам

Нет колонок, превышающих порог.

## Колонки
См. `summary.csv`.

## Пропуски
См. `missing.csv` и `missing_matrix.png`.

## Корреляции
См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки
Сохранены топ-5 значений в каталоге `top_categories/`.

## Гистограммы
Сохранено до 6 числовых гистограмм. Файлы `hist_*.png`.
